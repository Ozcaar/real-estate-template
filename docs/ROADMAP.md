# Roadmap

This document summarizes the current implementation state of the Real Estate Website Template, the active task, and the upcoming sequence. It is the canonical entry point for resuming work in a new OpenCode session.

For architecture, theming, data models, i18n rules, and component conventions, see the topic-specific docs in `docs/`. For per-implementation details and the rationale behind individual decisions, see the JSDoc on the corresponding source file. This file is intentionally short and summarises state; it does not duplicate larger docs.

## 1. Project Purpose

A modular, configuration-driven real estate website template that can be rebranded to any agency without editing components. Public site only for the MVP — properties, developments, agents, contact, and lead capture UI over static data. See `docs/SPEC.md` and `docs/REBRANDING.md` for the full scope and the rebrand workflow.

## 2. Current Implementation State

The MVP public site is functional end-to-end with static data.

| Area | State |
| --- | --- |
| Stack and docs scaffolding (`AGENTS.md`, `docs/*.md`, `package.json`, `nuxt.config.ts`) | Complete |
| Theme system + CSS variables + `useSiteConfig` + agency/theme/i18n config | Complete |
| Layout shell (`AppHeader`, `AppFooter`, `AppNavbar`, `AppMobileMenu`, `AppLanguageSwitcher`, `AppLogo`) | Complete |
| UI primitives (`BaseButton`, `BaseInput` area, `BaseCard`, `BaseContainer`, `BaseHeading`, `BaseIcon`, `BaseSection`, `BaseBadge`) | Complete |
| Shared components (`ResponsiveImage`, `CurrencyText`, `SectionHeader`, `CtaBlock`, `SocialLinks`, `FeatureItem`) | Complete |
| Feature components (`PropertyCard`, `PropertyGrid`, `PropertyGallery`, `DevelopmentCard`, `AgentCard`, `Home*`) | Complete |
| Pages: `/`, `/properties`, `/properties/[slug]`, `/developments`, `/agents`, `/about`, `/contact` | Complete |
| i18n (English + Spanish locale files, `useI18n` everywhere) | Complete |
| SEO (`usePageSeo`, canonical URLs, `og:url`, OG/Twitter cards, `NUXT_PUBLIC_SITE_URL`) | Complete |
| JSON-LD structured data (`useJsonLd`, per-page `RealEstateAgent` / `ItemList` / `RealEstateListing` / `ContactPage` / `AboutPage`) | Complete |
| Dynamic `/sitemap.xml` and `/robots.txt` Nitro routes (gated by `agency.modules.*`) | Complete |
| Listing query / sort / filter service (`propertiesService.filter`, accent-insensitive, stable order) | Complete |
| Mobile filter collapse (`< sm` viewport) | Complete; explicit hydration gate keeps SSR + no-JS form visible |
| Property sample-data runtime validation (`propertySchema`, `propertyListSchema`) | Complete; `coverImage` and required strings are non-empty |
| Lead capture pipeline (`/contact` form + `POST /api/contact` + 3 adapters) | Shipped (v1.1.0 M1). `LeadForm` is driven by `agency.leads.enabled` (default `false`); server-only runtime config picks the delivery adapter (`disabled` default, `log`, `webhook`); the endpoint maps the 8 transport responses (200, 200, 400, 413, 415, 429, 502, 503); honeypot + 16 KB body limit + 5-per-10-minute per-process rate limit. The contact methods column is always available as the no-JS and failed-delivery fallback. Post-merge verification (Task 083) confirmed all transport responses and adapter branches. |
| Per-development detail page (`/developments/[slug]`) | Not implemented |
| Per-agent detail page (`/agents/[slug]`) | Not implemented |
| Lightbox / advanced property gallery | Not implemented |
| Pagination | Complete; `BasePagination` UI primitive + generic `paginate` utility shipped (Task 071) |
| Breadcrumbs | Complete on the property detail page; `SeoBreadcrumbs` + `BreadcrumbList` JSON-LD shipped (Task 070) |
| Structured `PostalAddress` data on agency / property | Not implemented |
| Admin / dashboard / auth / backend / CRM | Not implemented (out of MVP scope) |
| **v1.0.0 release** | **Released** on the `release/v1.0.0` branch (tag `v1.0.0` at `456284c`). The 7 MVP pages, the static / theme / i18n / SEO infrastructure, the property gallery carousel, the listing filter + sort + pagination, the breadcrumbs, the structured `PostalAddress` companion, the Zod-validated sample data, the dynamic `/sitemap.xml` and `/robots.txt` Nitro routes, and the JSON-LD coverage are all shipped. `docs/RELEASE_NOTES_v1.0.md` records the release; the `release/v1.0.0` branch is **not** to be force-pushed or re-tagged. |
| **v1.1.0 development** | **In development** on `feature/lead-capture-v1.1`. The v1.0.0 release branch was merged into the lead-capture branch (commit `443890b`, "Merge v1.0.0 into lead capture v1.1"); the lead-capture code (`app/features/leads/`, `server/api/contact.post.ts`, `server/services/leads/`) is the first v1.1.0 milestone (v1.1.0 M0 / M1). The visible form is gated by `agency.leads.enabled` and defaults to `false` in the sample agency, so a rebrand ships the same placeholder behavior as v1.0 until it explicitly opts in. |

### Notes on the current state

* `app/app.vue` is no longer the `<NuxtWelcome />` placeholder — it renders `<NuxtRouteAnnouncer>` inside `<NuxtLayout>` and hosts `<NuxtPage>`. The page tree, layout, and feature modules are wired up.
* The current source-of-truth state lives in the "current implementation state" table in Section 2 and the per-milestone summaries in Section 3. The "Notes on the current state" block is a snapshot taken at each roadmap update; the live state of `git status` and `git log` is the single source of truth at any given moment.
* **v1.0.0 is released.** The `v1.0.0` tag is on the `release/v1.0.0` branch at commit `456284c`. The release branch is the canonical v1.0.0 state and is **not** to be force-pushed or re-tagged. See `docs/RELEASE_NOTES_v1.0.md` for the full release notes.
* **v1.1.0 is in development on the `feature/lead-capture-v1.1` branch.** The first v1.1.0 milestone is the lead-capture pipeline (v1.1.0 M0 / M1). The lead-capture code ships a real `POST /api/contact` endpoint with three pluggable server-only delivery adapters (`disabled`, `log`, `webhook`); the visible form is gated by `agency.leads.enabled` and defaults to `false` in the sample agency, so a rebrand ships the same placeholder behavior as v1.0 until it explicitly opts in. The contact methods column (tel / mailto / WhatsApp) is always available as the no-JS and failed-delivery fallback.
* `git status` is clean on the lead-capture branch. HEAD is `443890b` ("Merge v1.0.0 into lead capture v1.1"); the merge brought in `AGENTS.md`, `README.md`, `docs/REBRANDING.md`, `docs/RELEASE_NOTES_v1.0.md`, and `docs/SPEC.md` from the release branch. The v1.1.0 lead-capture code (commit `343abeb` and the post-merge verification) is on top of the v1.0.0 baseline.
* Validation pipeline (`pnpm lint`, `pnpm build`) passes. `pnpm lint` reports 0 errors and 0 warnings. `pnpm build` completes successfully (a known unrelated `@nuxt/image` warning about missing `sharp` binaries for `win32-x64` is emitted; it is not a build failure). Targeted runtime checks against `POST /api/contact` confirm the 8 transport responses (200, 200, 400, 413, 415, 429, 502, 503), the disabled / log / webhook adapter branches, the honeypot, and the 5-per-10-minute per-process rate limit.

### Milestone-divergence note

The release branch `release/v1.0.0` and the preserved branch `feature/lead-capture-v1.1` were developed in parallel. Both branches had entries numbered **M25** (lead-capture architecture audit, Task 079) and **M26** (real lead capture build, Task 080) — but they referred to the same work. After the v1.0.0 tag was cut, the lead-capture branch's M25 / M26 entries were carried forward as the first v1.1.0 milestone, and the release branch's M25 / M26 entries remain on the release branch as part of the v1.0.0 release history.

After merging v1.0.0 into the lead-capture branch (commit `443890b`), the milestone divergence is **reconciled** as follows:

* The lead-capture branch's lead-capture audit and build are now numbered **v1.1.0 M0 / M1** (the first v1.1.0 milestone). The historical M25 / M26 numbering on the lead-capture branch is preserved in the merged history for traceability but is superseded by the v1.1.0 numbering.
* The release branch's M25 / M26 entries remain on the `release/v1.0.0` branch and are part of the v1.0.0 release. They are **not** renumbered (the release branch is not to be force-pushed or re-tagged).
* The `docs/RELEASE_NOTES_v1.0.md` "Milestone-divergence note" is now historical context: the divergence it described has been reconciled by the merge.

## 3. Completed Milestones

The following milestones are committed to the repository. Treat them as the baseline; do not re-implement them in upcoming tasks.

> **Milestone numbering.** M0–M26 are the v1.0.0 milestones (shipped on the `release/v1.0.0` branch, tag `v1.0.0`). **v1.1.0 M0 and M1 are the first v1.1.0 milestones** (the lead-capture audit and build, carried over from the pre-merge M25 / M26 numbering on this branch — see the "Milestone-divergence note" in Section 2). Future v1.1.0 milestones will continue from M2.

1. **M0 — Project bootstrap.** Nuxt 4 with the `app/` directory convention, Tailwind CSS v4 via `@tailwindcss/vite`, Pinia, Zod v4, `@nuxtjs/i18n` (English + Spanish), Nuxt Image, Nuxt Icon, VueUse, Swiper. ESLint auto-resolved by `@nuxt/eslint`. Documentation set in `docs/`, `AGENTS.md`, `package.json` scripts (`dev`, `build`, `generate`, `preview`, `lint`, `lint:fix`).
2. **M1 — Theme + branding + i18n foundation.** `app/themes/default.theme.ts` + `app/themes/index.ts` (token-to-CSS-variable resolution), `app/config/agencies/default.agency.ts`, `app/config/site.config.ts`, `app/config/navigation.ts`, `app/config/seo.ts`, `app/config/i18n.ts`, `app/composables/useSiteConfig.ts`, and the matching `app/types/*.types.ts`. The active theme id is applied as `data-theme` on `<html>`.
3. **M2 — Layout shell and UI primitives.** `AppHeader`, `AppNavbar`, `AppMobileMenu`, `AppLanguageSwitcher`, `AppLogo`, `AppFooter`; `BaseButton`, `BaseIcon`, `BaseCard`, `BaseContainer`, `BaseSection`, `BaseHeading`, `BaseBadge`. All consume theme tokens (`var(--color-*)`, `var(--radius-*)`); no hardcoded brand colors.
4. **M3 — Home page.** `HomeHero`, `HomePropertySearch`, `HomeSearchBar`, `HomeFeaturedProperties`, `HomeCategories`, `HomeLocations`, `HomeServices`, `HomeAbout`, `HomeTestimonials`, `HomeContactCta` — all reading data from `app/features/home/data/*`.
5. **M4 — Properties module (catalog + detail).** `features/properties/{components,constants,data,schemas,services,types}/`. `PropertyCard`, `PropertyGrid`, `PropertyGallery`, the listing page (`/properties`) with filter form, the detail page (`/properties/[slug]`) with 404 handling, related properties, area/measurement rendering, and per-property `RealEstateListing` JSON-LD.
6. **M5 — Developments module (catalog only).** `features/developments/{components,data,types}/`, `DevelopmentCard`, the listing page (`/developments`). No detail page yet — the `Development.slug` field is reserved for the future route.
7. **M6 — Agents module (catalog only).** `features/agents/{components,data,types}/`, `AgentCard`, the listing page (`/agents`) with `ItemList` JSON-LD.
8. **M7 — Contact + lead capture UI.** `/contact` page with agency contact methods, WhatsApp CTA, and a placeholder form (no backend). `ContactPage` JSON-LD. Lead feature folder reserved at `app/features/leads/`.
9. **M8 — SEO refactor + sitemap/robots.** Per-page `usePageSeo` + `useSeoMeta` + canonical URLs gated by `NUXT_PUBLIC_SITE_URL`; dynamic `/sitemap.xml` and `/robots.txt` Nitro routes in `server/routes/`, gated by `agency.modules.*` and the same env var.
10. **M9 — JSON-LD structured data.** `core/composables/useJsonLd.ts`, plus per-page payloads: `RealEstateAgent` (home, shared `@id` from the home page absolute URL), `ItemList` of `ListItem` (properties catalog), `RealEstateListing` + `ItemList` of related properties (property detail), `ItemList` of `Person` (agents), `ContactPage` and `AboutPage` with `mainEntity: RealEstateAgent` (sharing the same `@id`).
11. **M10 — Property listing service hardening.** `propertiesService` with `getAll`, `getBySlug`, `getFeatured`, `getRelated` (weighted score), and `filter` (operation / type exact match, location substring against `location + city + state + country` with case-insensitive and accent-insensitive Unicode NFD normalization, stable sort tiebreak on `id` ascending). Type guard `isPropertySort`, canonical URL strips the query string, unknown `?sort=` values are coerced to `featured`. Documented in `docs/DATA_MODELS.md` Section 9.
12. **M11 — Property schema hardening.** `property.schema.ts` (`propertySchema`, `propertyListSchema`) with a compile-time guard against the `Property` interface. `coverImage` and all required string fields are `.min(1)`; `images` and `amenities` are non-empty per entry; the schema is the runtime boundary used to validate the static catalog at module load.
13. **M12 — Mobile filter responsiveness.** The `/properties` filter form is collapsed by default on `< sm` viewports behind a "Filters" toggle button that reflects the active filter count, and the form is always visible on `sm+`. The form retains `method="get" action="/properties"` so it submits natively.
14. **M13 — Mobile filter SSR / hydration fix (Task 067).** Added an explicit `isHydrated` gate in `app/pages/properties/index.vue`. The form uses `v-show="!isHydrated || !isMobile || isFilterOpen"` so the SSR HTML and no-JavaScript clients always render the form. The mobile toggle uses `v-if="isHydrated && isMobile"` so it only enters the DOM after the client takes over AND the viewport is in the mobile range. `isHydrated` is flipped to `true` at the end of `onMounted`, so the first client-side reactive update uses the real `matchMedia` value with no flash. The form still uses `v-show` (not `v-if`) so its DOM and selected values are preserved across collapse/expand. The same commit fixed the eight pre-existing `vue/first-attribute-linebreak` warnings on the file.
15. **M14 — Pagination audit (Task 068).** Pagination is part of the v1.0 template scope and is **not** deferred because the current placeholder catalog is small. The placeholder record count is not a reason to skip the build; the template must be production-ready for any agency on day one. The agreed architecture is: URL state via `?page=N`; page `1` is omitted from generated URLs; invalid `?page=` values (missing, empty, zero, negative, decimal, array, out-of-range, non-numeric) normalize safely; filter changes reset to `?page=1`; sort changes and pagination links preserve the existing `?operation=&type=&location=&sort=` query; `propertiesService.filter()` is unchanged; the page layer paginates the local placeholder data; the pagination UI must be reusable for a future API or CMS source. The build target is a reusable `BasePagination` UI primitive plus a small generic pagination utility. The component must render nothing when `totalPages <= 1`.
16. **M15 — Breadcrumb audit (Task 069).** Visible breadcrumbs and `BreadcrumbList` JSON-LD land on `/properties/[slug]` only. Listing pages (`/properties`, `/developments`, `/agents`) and the top-level pages (`/`, `/about`, `/contact`) do not get visible breadcrumbs in v1.0 because they are themselves top-level destinations. Reusable `SeoBreadcrumbs` shared component + typed `BreadcrumbItem` + `BreadcrumbList` JSON-LD via the existing `useJsonLd`. The component renders `null` when `items.length <= 1`. The trail is `Home › Properties › {property.title}`. i18n strategy is `no_prefix` so breadcrumb URLs are bare paths; `toAbsoluteUrl()` handles the absolute-URL conversion.
17. **M16 — Breadcrumbs MVP (Task 070).** New shared component `app/components/shared/SeoBreadcrumbs.vue` + new `BreadcrumbItem` type in `app/types/breadcrumb.types.ts` + new `common.breadcrumb` i18n key (English `Breadcrumb`, Spanish `Migas de pan`). Wired into `app/pages/properties/[slug].vue` with a three-item trail (`Home › Properties › {property.title}`) and a third JSON-LD payload via the existing `useJsonLd` composable — a `BreadcrumbList` with 1-based positions, absolute URLs via `toAbsoluteUrl`, and the current item using `canonicalUrl.value` with a `toAbsoluteUrl('/properties/${slug}')` fallback. The existing `RealEstateListing` and related-properties `ItemList` JSON-LD payloads are unchanged. The standalone "Back to properties" block on the property detail page is removed because the breadcrumb's second item points to the same destination. No breadcrumbs are added to listing or top-level pages. `docs/REBRANDING.md` Section 14 documents the trail, the reusable component, the `common.breadcrumb` key, and the future reuse on agent and development detail pages.
18. **M17 — Pagination MVP (Task 071).** New generic `paginate` utility at `app/core/utils/paginate.ts` with `PaginatedResult<T>`, `parsePageParam(raw)`, and `paginate(items, page, pageSize)`. New UI primitive `app/components/ui/BasePagination.vue` with `currentPage`, `totalPages`, `basePath`, `query`, `siblings`, `boundaries` props. Renders `null` when `totalPages <= 1`. Accessible markup: `<nav aria-label>`, `<ol>` list, `aria-current="page"` on the current page, `aria-disabled="true"` non-focusable previous / next at the boundaries, decorative `aria-hidden` ellipses, and a `sr-only` live region announcing the current page. URL contract: `?page=1` is omitted, later pages get `?page=N`, empty query values are stripped, and `?page=99` on a 3-page set silently clamps to page 3 (no 404, no empty grid). `propertiesService.filter()` is unchanged; pagination is a page-layer concern applied AFTER filtering and sorting. The listing-page `ItemList` JSON-LD uses global positions `(currentPage - 1) * PAGE_SIZE + index + 1` so `position` stays stable across pages. Filter and sort form submissions reset to page 1 (the form's submit handler builds a fresh query that omits `page`); "Clear filters" navigates to bare `/properties` and also strips `?page=N`. The property count/status UI shows the total filtered count, not the current page count. The placeholder catalog (6 records) renders exactly as before with no pagination control visible. New i18n keys: `properties.pagination.{nav, previous, next, page, currentPage, pageStatus}` in both locales. `docs/DATA_MODELS.md` Section 9 documents the `page` key, the omission of `?page=1`, the invalid-value normalisation, the form-reset behaviour, and the pagination-link preservation.
19. **M18 — PostalAddress audit (Task 072).** Agency and property addresses are separate concerns. The agency address is a business location (a real street, postal code, and country); the property address may be partial for privacy or marketing reasons. The placeholder property catalog has no street addresses and no postal codes, so `RealEstateListing.address` is intentionally **deferred** to a future task when a real agency provides structured per-property addresses. The agency build is migration-safe: a new optional `contact.structuredAddress` companion is added; the existing free-text `address` stays the visible display value; the three agency JSON-LD blocks (`RealEstateAgent` on home, `mainEntity.RealEstateAgent` on contact, `mainEntity.RealEstateAgent` on about) keep the same `RealEstateAgent.@id` and switch to a `PostalAddress` shape when the structured fields are present, falling back to the plain string otherwise.
20. **M19 — PostalAddress build (Task 073).** New `AgencyStructuredAddress` type in `app/types/agency.types.ts` with five optional schema.org field names (`streetAddress`, `addressLocality`, `addressRegion`, `postalCode`, `addressCountry`). Extended `AgencyContactConfig` and `agencyContactConfigSchema` with the optional `structuredAddress` field. New `agencyPostalAddress(agency)` helper at `app/core/utils/postal-address.ts` (~60 lines, no Vue / i18n / route / page dependency) that returns a `PostalAddress` object with only the non-empty fields, or falls back to the plain `contact.address` string. Updated `app/pages/index.vue`, `app/pages/contact/index.vue`, and `app/pages/about/index.vue` to call the helper — the three `RealEstateAgent` nodes still share the same `toAbsoluteUrl('/')` `@id`. Default agency config in `app/config/agencies/default.agency.ts` adds `structuredAddress` parsed from the existing free-text (`streetAddress: '123 Main Street'`, `addressLocality: 'Anytown'`, `addressCountry: 'USA'`); `addressRegion` and `postalCode` are not invented. The free-text `contact.address` is unchanged and still required. Property `RealEstateListing` JSON-LD is unchanged — property `PostalAddress` remains intentionally deferred. `docs/REBRANDING.md` Section 3a documents the new field, the five supported fields, the fallback behaviour, and the migration-safe contract. `docs/DATA_MODELS.md` Section 6 documents the `AgencyStructuredAddress` shape and the free-text-vs-structured responsibility.
21. **M20 — Property gallery enhancement audit (Task 074).** The current static thumbnail gallery satisfies basic browsing but has three real gaps: (1) no mobile swipe between images, (2) no keyboard arrow-key navigation, (3) no prev/next controls on desktop. Swiper v12 (already a dependency per `package.json:21`) addresses all three with a single `swiper/vue` integration. The fullscreen lightbox is **intentionally deferred** to a future audit: the current main image is `sizes="100vw lg:50vw"` (large enough for the MVP), the placeholder data does not justify a generic modal system, and a real-estate user wanting a larger view can use the browser's built-in image controls. The carousel MVP covers the three real gaps without the complexity cost of a focus trap, body-scroll lock, Escape handler, and backdrop click. The build is Task 075.
22. **M21 — Property gallery carousel build (Task 075, renamed from "Lightbox MVP").** Rewrote `app/features/properties/components/PropertyGallery.vue` with a Swiper 12 carousel using `import { Swiper, SwiperSlide } from 'swiper/vue'` and the `Keyboard`, `A11y` modules from `swiper/modules` (the `Navigation` module is intentionally not imported — the custom prev/next controls call `swiper.slidePrev()` / `swiper.slideNext()` directly). CSS imports: `swiper/css`, `swiper/css/a11y`, `swiper/css/keyboard`. Custom real `<button type="button">` previous and next controls that call `swiper.slidePrev()` / `swiper.slideNext()` directly, with the native `disabled` attribute at the boundaries (no loop, no rewind). Swipe and drag on touch + mouse, keyboard arrow navigation enabled only in the viewport (`onlyInViewport: true`), and a visible translated image counter rendered as a plain `<p>` for sighted users (Swiper's own `.swiper-notification` element, with `wrapperLiveRegion: true` by default, is the single screen reader announcement source — a second `aria-live` on the counter would announce the same slide change twice and has been deliberately avoided). The existing thumbnail strip is preserved as a custom `<ul>` of `<button>` elements; clicking calls `swiper.slideTo(index)` and updates `aria-current="true"` on the active thumbnail. The A11y module is configured with translated `prevSlideMessage`, `nextSlideMessage`, `firstSlideMessage`, `lastSlideMessage`, `slideLabelMessage`, and `containerRoleDescriptionMessage`. The `<ClientOnly>` wrapper with a `#fallback` slot renders a plain `ResponsiveImage` during SSR so the first image is in the initial HTML with `loading="eager"` and `fetchpriority="high"`; the Swiper replaces it after hydration using the same `src` (browser cache prevents a duplicate download). Later main images and thumbnails use `loading="lazy"`. `prefers-reduced-motion: reduce` sets Swiper's `speed` to 0 via VueUse's `usePreferredReducedMotion`. `loop` and `rewind` are disabled. The `app/pages/properties/[slug].vue` call site is unchanged (same props). New i18n keys under `properties.detail.gallery`: `ariaLabel`, `previous`, `next`, `first`, `last`, `slideLabel`, `counter` in both `en.json` and `es.json`; the existing `viewImage` and `thumbnails` keys are reused. `docs/REBRANDING.md` Section 15 documents the carousel behaviour, the LCP contract, the lazy-loading rules, **Layout-containment regression correction (before Task 075 closed):** CSS grid items default to `min-width: auto`, which let the Swiper's intrinsic slide width expand the `lg:grid-cols-2` column beyond its `1fr` share and produce an extremely large width. The fix adds `min-w-0 w-full max-w-full` to the `<figure>` root of `PropertyGallery.vue`, wraps the `<ClientOnly>` in a `<div class="w-full min-w-0 max-w-full overflow-hidden">`, and adds `w-full min-w-0 max-w-full overflow-hidden` to the `<Swiper>` root. Each level of the containment chain applies the same three classes so a single `display: contents` ancestor or a future refactor cannot silently remove them. The SSR fallback inherits the same constrained width. The fix does not change the carousel behaviour, the image-loading contract, the accessibility implementation, the image sizes attribute, the 4/3 ratio, or the responsive layout. No arbitrary fixed or viewport-unit width was introduced. the reduced-motion handling, the deferred lightbox, and the full translation key table. Property `RealEstateListing` JSON-LD, the property model, the property schema, and the sample data are all unchanged.

23. **M22 — Documentation and accessibility review (Task 076).** A static, source-level review of the v1.0 implementation completed through Task 075. The review cross-references `AGENTS.md`, `docs/ROADMAP.md`, `docs/DESIGN.md`, `docs/DATA_MODELS.md`, `docs/REBRANDING.md`, the actual source files (pages, gallery, pagination, breadcrumbs, postal-address, usePageSeo, useJsonLd), and the i18n locale files. Scope: documentation consistency, accessibility markup (navigation landmarks, form labels, button names, keyboard focus, disabled controls, `aria-current`, live regions, decorative icons, image alternative text, mobile touch targets, reduced-motion), SEO and JSON-LD consistency (canonical URLs, stable `RealEstateAgent.@id`, `ContactPage`, `AboutPage`, `RealEstateListing`, listing and related-property `ItemList`, `BreadcrumbList`, agency `PostalAddress`), responsive source behaviour at narrow / tablet / two-column / very-wide widths, and English / Spanish i18n parity. The review explicitly did NOT run a browser-based accessibility audit: no axe, Lighthouse, Playwright, or runtime interaction audit was performed because those tools are not configured in the repository and adding them was out of scope for this low-risk pass. `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. Three low-risk documentation fixes were applied: (1) `docs/REBRANDING.md` Section 15 no longer claims the visible counter uses a polite live region — it is a plain `<p>` for sighted users and Swiper's own `.swiper-notification` element is the single screen reader announcement source; (2) `docs/ROADMAP.md` "Notes on the current state" and "Working tree" were updated to reflect the current commit (`2abf940`, M21) and the clean working tree, replacing a stale reference to commit `80d21af` (M19) and a stale claim of in-flight uncommitted changes; (3) the M21 milestone description and Section 4 active task description were corrected to match the actual implementation (no `Navigation` module import, no `swiper/css/navigation` import, no polite live region on the counter). No runtime code was modified. AGENTS.md was not changed because its Project State and Current Gaps sections remain accurate.
24. **M23 — MVP completeness audit (Task 077).** A static, source-level review of the v1.0 implementation completed through Task 076. The audit inventory-checked every route and capability against `docs/SPEC.md` §6 (MVP Scope) and the documented scope, walked the primary user journeys (arrive on home → browse and filter → paginate and sort → open a property → browse its gallery → view related properties → inspect agents / developments → learn about the agency → contact the agency) to confirm each has a usable beginning, middle and completion state, and audited the rebrand path, contact flow, SEO, and production-readiness. The review verified that `pnpm lint` reports 0 errors and 0 warnings and `pnpm build` completes successfully. **Verdict: no release blockers.** The five top-level pages beginning with an h2 (no page-level h1) was classified as a heading-structure and semantic-quality concern, NOT as an automatic WCAG failure. The non-functional contact form was classified as an acceptable documented template placeholder because the contact methods column already provides a fully functional completion path through `tel:`, `mailto:`, and `https://wa.me/`. The fullscreen property-gallery lightbox and real external data integration are explicitly out of MVP scope per `docs/SPEC.md` §7 and were recorded as intentionally deferred. The recommended next task (Task 078) is a small, three-file v1.0 pre-release build.
25. **M24 — Final v1.0 pre-release fixes (Task 078).** Three local changes that close the only release-time findings from the M23 audit. (1) Five page-level heading fixes: added an explicit `:level="1"` to the first, page-level `<SectionHeader>` on each of `/properties`, `/developments`, `/agents`, `/about`, and `/contact`. `SectionHeader`'s default level is unchanged at `2`; only the first SectionHeader on each top-level route is promoted. Nested `SectionHeader` sub-section headings on `/about` (story → values → stats) keep their existing h2 level. (2) SEO config cleanup: removed the dead `titleTemplate` property from `app/config/seo.ts` and the matching `titleTemplate` member from the `SeoConfig` interface in `app/types/site.types.ts`. Every page sets its title directly through `useSeoMeta({ title: ... })`; the template was declared but never consumed. The actual title output, page-title translations, and title formatting are unchanged. (3) Doc consistency: the matching `SeoConfig` reference in `docs/DATA_MODELS.md` §8 was updated to match the new shape. `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. No release blocker remains from the M23 findings. **`SectionHeader`'s default level remains `2`.** The home page (`HomeHero`) and the property detail page (`app/pages/properties/[slug].vue`) retain their existing `<h1>` elements unchanged. **`~/core/composables` remains in `nuxt.config.ts → imports.dirs` and `usePageSeo` / `useJsonLd` continue to require explicit imports** (page files already import both explicitly). **Template is ready for a version 1.0 release.**
26. **M25 — Lead capture architecture audit (Task 079).** A static, source-level audit of the current `/contact` form and a small, secure, configurable lead-capture architecture for the first post-v1.0 feature. Scope: audit current form behavior (4 fields, `disabled` submit, placeholder notice), confirm `app/features/leads/` and any server API structure are absent, define the smallest reusable `LeadInput` contract (name / email / phone / message / honeypot / optional locale — no property, development, interest, IP, or user-agent), define a Zod schema with permissive phone formatting, define the Nitro endpoint contract (`POST /api/contact`, JSON-only, 400 / 413 / 415 / 429 / 502 / 503), define a pluggable delivery adapter boundary, evaluate four adapter options (development / log / webhook / email), recommend a no-persistence `log` + `webhook` build (no SDK, no paid service, no filesystem storage), recommend spam / privacy / server-security defaults (honeypot, 16 KB body limit, in-memory per-process rate limit, no CAPTCHA, no PII in logs), define the five-state client machine and accessibility requirements, identify no-JS / failed-delivery fallbacks as the existing tel / mailto / WhatsApp column, recommend environment + rebranding configuration without secrets in public runtime config, and recommend the exact Task 080 build scope. **Verdict: a no-PII, no-dependency, no-persistence lead-capture build is the correct v1.0.1 add-on. The disabled-by-default server adapter is the safe default; the visible form's `leads.enabled` agency flag is the rebrand knob; the contact methods column remains the no-JS and failed-delivery fallback.** _Historical: this work is now numbered **v1.1.0 M0** on the lead-capture branch after the v1.0.0 merge — see the "Milestone-divergence note" in Section 2._
27. **M26 — Real Lead Capture (Task 080).** Implements the M25 architecture. New files: `app/features/leads/{types/lead.types.ts,schemas/lead.schema.ts,components/LeadForm.vue}` and `server/services/leads/{delivery-adapter.ts,lead.service.ts}` plus `server/services/leads/adapters/{disabled.ts,log.ts,webhook.ts,index.ts}` and `server/api/contact.post.ts`. Modified files: `nuxt.config.ts` (server-only `leadsAdapter` / `leadsWebhookUrl` / `leadsWebhookSecret` runtime config), `app/types/agency.types.ts` + `app/config/agencies/agency.schema.ts` + `app/config/agencies/default.agency.ts` (new `leads.enabled: boolean` flag, default `false` in the sample agency), `app/pages/contact/index.vue` (embeds `<LeadForm>` and removes the inlined form), `i18n/locales/{en,es}.json` (new `contact.form.{success,error,honeypotLabel,errors.*}` keys), `docs/DATA_MODELS.md` §5 (promoted from "planned" to "shipped" with the input contract, field rules, stamped shape, honeypot, and endpoint response table), `docs/REBRANDING.md` §12 (new section covering form interactivity, server-only runtime config, webhook signature, deployment requirements, no-JS / failed-delivery fallback, privacy stance, rebrand checklist, troubleshooting), `docs/ROADMAP.md` (this entry). The shared Zod schema lives in `app/features/leads/schemas/lead.schema.ts` and is re-validated server-side on every request. The endpoint rejects `application/json` mismatches with 415, oversized bodies with 413, malformed JSON with 400, and excess submissions with 429 (5 accepted attempts per 10 minutes per request key, in-memory `Map`, opportunistic cleanup, **not** distributed). The disabled adapter returns 503; the log adapter writes a single redacted `console.info` line (no PII); the webhook adapter signs the exact JSON payload with HMAC SHA-256, uses a 5-second `AbortController` timeout, does not follow redirects, and never exposes the upstream response. `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. No new dependencies. The contact methods column is unchanged and remains the no-JS / failed-delivery fallback. _Historical: this work is now numbered **v1.1.0 M1** on the lead-capture branch after the v1.0.0 merge — see the "Milestone-divergence note" in Section 2._

28. **v1.1.0 M0 — Lead capture architecture audit (Task 079, post-merge).** The lead-capture architecture audit (originally numbered M25 on this branch before the v1.0.0 merge) is the first v1.1.0 milestone. The audit's recommendation is the basis for v1.1.0 M1. The historical M25 numbering is preserved in the merged history for traceability but is superseded by the v1.1.0 M0 numbering.
29. **v1.1.0 M1 — Real Lead Capture build + post-merge integration verification (Task 080, Task 083).** The real lead-capture build (originally numbered M26 on this branch before the v1.0.0 merge) plus the post-merge integration verification (Task 083) form the second v1.1.0 milestone. The build ships `app/features/leads/{types,schemas,components}/`, `server/services/leads/{delivery-adapter,lead.service}.ts`, `server/services/leads/adapters/{disabled,log,webhook,index}.ts`, and `server/api/contact.post.ts`. The endpoint maps the 8 transport responses (200, 200, 400, 413, 415, 429, 502, 503), the three pluggable server-only delivery adapters (`disabled`, `log`, `webhook`) are selected at request time, the visible form is gated by `agency.leads.enabled` (default `false` in the sample agency), the honeypot + 16 KB body limit + 5-per-10-minute per-process rate limit are in place, and the contact methods column is always available as the no-JS and failed-delivery fallback. The post-merge verification (Task 083) runs the endpoint against the eight transport responses, the disabled / log / webhook adapter branches (including webhook signing, 401 / 403 / 5xx, timeout, network error, manual redirect), the honeypot silent-200 path, the 5-per-10-minute per-process rate limit (validation failures do not consume the budget), and the English / Spanish i18n parity for the form labels, placeholders, placeholder notice, honeypot label, success message, error message, and per-field error messages. One concrete issue was found and fixed: `LeadForm.vue`'s template `ref="nameRef"` etc. were not bound to the script's `fieldRefs` map, so `focusFirstInvalid()` could not focus the invalid field after a server-side validation failure. Fix: declare the individual `ref()` instances in the script (`nameRef`, `emailRef`, `phoneRef`, `messageRef`, `honeypotRef`) and have the `fieldRefs` map reference them. `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. The historical M26 numbering is preserved in the merged history for traceability but is superseded by the v1.1.0 M1 numbering.


30. **v1.1.0 M2 — Vitest Foundation (Task 084).** A small, maintainable Vitest foundation plus the permanent automated tests for the lead-capture pipeline. New file: `vitest.config.ts` (Node environment, test patterns under `app/features/leads/schemas/**` and `server/services/leads/**`, an alias for Nuxt's `#imports` pointing to a tiny stub at `tests/stubs/imports.ts` so the adapter pipeline can be exercised without booting a Nitro server). New test files: `app/features/leads/schemas/lead.schema.test.ts` (37 cases covering every field rule plus the cross-field contact channel requirement), `server/services/leads/adapters/disabled.test.ts` (3 cases — id, errorCode, no-throw on empty lead), `server/services/leads/adapters/log.test.ts` (9 cases — id, ok:true, single `console.info` per lead, the exact log line shape, no PII in the log line, and the field-presence / message-length fallbacks), `server/services/leads/adapters/webhook.test.ts` (18 cases — id, 200 / 401 / 403 / 500 / 502 / 503 / 3xx / network error / AbortError / missing config × 3, the exact JSON payload, content-type, X-Lead-Signature header with the HMAC SHA-256 hex digest, `redirect: 'manual'`, and the AbortSignal for the 5-second timeout), `server/services/leads/adapters/index.test.ts` (9 cases — registry exports, selection by id, missing / unknown / whitespace ids, default fallback to disabled), and `server/services/leads/lead.service.test.ts` (28 cases — honeypot silent path with no adapter call, schema validation mapping, rate limit (5-per-window cap, 6th blocked, validation failures and honeypot trips do not consume the budget, request keys are tracked independently), lead stamping (UUID id, ISO receivedAt, source "contact", forwarded fields, locale fallback), delivery mapping (ok / disabled / transport / auth / rate_limited / unsupported), and non-object body rejection (string, null, array)). Modified files: `package.json` (added `vitest` to devDependencies; added `test` and `test:watch` scripts), `docs/ROADMAP.md` (this entry). Total: 104 tests across 6 test files; three consecutive `pnpm test` runs pass consistently; `pnpm lint` and `pnpm build` also pass. No runtime source code was modified. The `#imports` alias and the small `tests/stubs/imports.ts` stub are the only test-infrastructure additions; no production code path was touched.


31. **v1.1.0 M3 — v1.1.0 documentation parity (Task 085).** A documentation-only pass that brings the lead-capture, testing, and agency-config docs into exact parity with the current v1.1.0 implementation, the Vitest foundation, and the running code. No runtime code was changed. New / updated documentation: `AGENTS.md` (Current Gaps entry rewritten to reflect the v1.1.0 development state and the Vitest foundation; Package manager section adds `pnpm test` and `pnpm test:watch`), `README.md` (Scripts section adds the two test scripts; Deployment modes section reflects v1.0.0 is released and v1.1.0 ships the `POST /api/contact` endpoint), `docs/REBRANDING.md` §12 (four new subsections: webhook response → transport mapping, endpoint transport behavior, automated tests; refined rate-limit description; corrected Unicode em-dash display in three lines; the misleading timeout-retryability comment in `server/services/leads/adapters/webhook.ts` is corrected in source — the comment now reflects the actual behavior: network errors and DNS failures are `retryable: true`, timeout aborts are `retryable: false`), `docs/DATA_MODELS.md` §5.5 (corrected the phone regex in the doc to match the source: `/^[+]?[0-9 ()-]{6,32}$/` without the `u` flag and without the escaped hyphen), §5.7 (expanded `Endpoint transport guards` subsection: method, content type, body size, JSON parse, with the exact trigger / status / body for each), §6 (added `AgencyLeadsConfig` interface and the `leads` field on `AgencyConfig` to match the hand-written `app/types/agency.types.ts`), `docs/ROADMAP.md` (this entry; Section 4 deduplication; Section 5 identifies Task 086 / v1.1.0 M4 as the next task). `pnpm test` reports 104/104 pass; `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. No runtime source code was modified. The only source-file change is the doc comment in `webhook.ts` (no behavior change).
## 4. Current Work


32. **v1.1.0 M4 — Endpoint + rate-limit window-expiry tests (Task 086).** Permanent Vitest coverage for the `POST /api/contact` endpoint transport guards and the rate-limit window-expiry behavior. New files: `server/api/contact.post.test.ts` (16 cases — valid submission, forwarded body / request key / fallback locale, 415 for missing / wrong Content-Type, 413 for body > 16 KB including the 16 KB + 1 byte boundary, 400 for empty / malformed JSON, 400 for schema validation with mapped issues, 200 silent success for honeypot, 429 for rate-limited, 503 for adapter-disabled, 502 for delivery failure, and content-type header on every response), `tests/helpers/h3-event.ts` (a minimal H3Event mock builder for the endpoint tests), and 5 new rate-limit window-expiry cases in `server/services/leads/lead.service.test.ts` (window expiry after 10 min, strict greater-than boundary at exactly 10 min, full 5-slot reset after expiry, opportunistic cleanup of stale entries for unrelated keys, and partial-window timestamp tracking across the 10-min boundary). Modified files: `package.json` + `pnpm-lock.yaml` (added `h3@^1.15.11` to devDependencies — `h3` is the Nitro HTTP framework already used at build time; declaring it explicitly follows the standard Nuxt pattern for testing Nitro endpoints without booting a server), `vitest.config.ts` (added `server/api/**/*.test.ts` to the include pattern). Total: 125 tests across 7 test files; 21 new tests added in M4 (16 endpoint + 5 rate-limit window-expiry). Three consecutive `pnpm test` runs pass consistently; `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. No runtime source code was modified. All previous tests still pass.
### Active task


33. **v1.1.0 M5 — SMTP email delivery adapter (Task 087).** A production-ready SMTP email delivery adapter for the lead-capture pipeline, with isolated private runtime configuration, automated tests, and documentation. New file: `server/services/leads/adapters/email.ts` (~190 lines — Nodemailer transporter creation, required-config validation before any SMTP connection, plain-text + HTML body rendering with full HTML escaping of every user-provided value, `replyTo` set only when the lead email is present, 5-second timeouts on `socketTimeout` / `connectionTimeout` / `greetingTimeout`, and SMTP error-code → `LeadDeliveryResult` mapping: `EAUTH*` → `auth`, `ETIMEDOUT` / `EAI_AGAIN` → `transport` (not retryable), everything else → `transport` (retryable), missing required config → `unsupported`). New test file: `server/services/leads/adapters/email.test.ts` (35 cases — `id`, config validation for every required field and the non-integer-port branch, successful delivery, plain-text and HTML body content, `replyTo` present when email is set / absent when empty, HTML escaping of `< > & " '`, em-dash placeholder for empty fields, newline → `<br>`, `secure` flag handling for `true` / `false` / empty, error mapping for `EAUTH` / `EAUTHENTICATION` / `ETIMEDOUT` / `EAI_AGAIN` / `ECONNECTION` / no-code / non-Error throw values, transporter lifecycle on success / failure / `close()` rejection). Modified files: `package.json` + `pnpm-lock.yaml` (added `nodemailer@^9.0.3` to `dependencies` and `@types/nodemailer@^8.0.1` to `devDependencies` — both follow the standard Nuxt 4 / TypeScript patterns; `nodemailer` is the only new runtime dependency), `nuxt.config.ts` (added 7 private runtime config keys: `leadsSmtpHost` / `leadsSmtpPort` / `leadsSmtpSecure` / `leadsSmtpUser` / `leadsSmtpPassword` / `leadsEmailFrom` / `leadsEmailTo`, all outside the `public:` block so they are never sent to the client bundle), `server/services/leads/delivery-adapter.ts` (added `'email'` to the `LeadDeliveryAdapter.id` union), `server/services/leads/adapters/index.ts` (registered the `emailAdapter`), `server/services/leads/adapters/index.test.ts` (added the `leadsAdapter === 'email'` test; the previous stale `'email'` unknown-id case was renamed to `'sms'`), `app/config/agencies/default.agency.ts` (the existing `leads.enabled: false` is unchanged; only the comment was expanded to list the four adapter values), `docs/REBRANDING.md` §12 (added §12.3b "Email (SMTP) adapter" with the full configuration table, validation, body, timeouts, and SMTP error → delivery-result mapping; updated §12.6 Privacy, §12.7 Rebranding checklist, §12.8 Troubleshooting, and §12.10 test table to include the email adapter), `docs/DATA_MODELS.md` §5 (status updated to "shipped (Task 080 / M26) + extended (Task 087 / M5)"; §5.6 endpoint response table updated), `docs/ROADMAP.md` (this entry; Section 4 deduplication; Section 5 identifies Task 088 / v1.1.0 M6 as the next task; Section 6 "Real lead capture" entry updated; Section 6 "Vitest unit-test foundation" entry updated; Section 8 step 3 updated). 36 new tests added in M5 (35 email + 1 adapter-selection). Total: 162 tests across 8 test files. Three consecutive `pnpm test` runs pass consistently; `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. All previous tests still pass. The only source code changes are the new `email.ts` adapter and the small registration touch-up; no behavior change in any existing module.


34. **v1.1.0 M6 — Test coverage expansion (Task 088).** A test-only pass that expands the Vitest coverage to the remaining pure-function / branch-rich surface in the repo: the generic `paginate` utility, the `parsePageParam` URL query normalizer, the `buildWhatsAppLink` phone-number helper, the `agencyPostalAddress` JSON-LD builder, the `propertiesService` (filter / sort / getAll / getBySlug / getFeatured / getRelated / isPropertySort), and the `agencyConfigSchema` (the structural rules, the cross-config coherence checks, the permissive format warnings, and the throwing vs. non-throwing helpers). No runtime code was changed. New test files: `app/core/utils/paginate.test.ts` (43 cases — every documented edge case of `parsePageParam` including `null`, `undefined`, `''`, whitespace, `'0'`, `'-3'`, `'abc'`, `'1.9'`, `'1e2'`, single-digit, exponential, trimmed, scalar non-string, empty array, array-with-undefined-first, multi-value array, decimal-in-array, and the `paginate` function on empty / single-item / underfilled / exact-fit / oversized lists, the pageSize coercion of `0` / negative / fractional, the request clamping of negative / zero / out-of-range / `NaN` / fractional, the input-immutability guarantee, the `readonly T[]` overload, and the empty-list short-circuit), `app/core/utils/whatsapp-link.test.ts` (21 cases — `null`, `undefined`, `''`, whitespace, no-digits, leading `+` strip, spaces, dashes, parentheses, dots, mixed-alphanumeric digit extraction, single / two-digit numbers, E.164 max length, oversized numbers (no length bound), internal-space stripping, exact-prefix invariant, and the explicit `null` (not `undefined`) return for empty input), `app/core/utils/postal-address.test.ts` (18 cases — every branch: legacy / unmigrated agency (no `structuredAddress`, empty object, all-empty fields, whitespace-only fields), migrated agency (single-field PostalAddress for every documented field, full PostalAddress with all five fields, partial fields with whitespace-padded values, undefined-field omission, `@type` invariant), and the fallback edge cases (custom free-text address, whitespace-only object fall-back to free-text)), `app/features/properties/services/properties.service.test.ts` (72 cases — `isPropertySort` allow-list and every rejection branch, `getAll` exclusion of hidden, `getBySlug` for known / missing / hidden / wrong-case / empty slugs, `filter` for every operation (sale / rent / case-insensitive / trimmed / empty / unknown), every type (house / apartment / land / commercial / office / case-insensitive / unknown), combined operation + type, location (city substring / country substring / accent-insensitive on `México` and `Querétaro` / slugified haystack / unknown / empty / undefined / trimmed), sort (`featured` with `id`-ascending stable tiebreak, `price-asc` non-decreasing with `id`-ascending tiebreak, `price-desc` non-increasing with `id`-ascending tiebreak, the asc/desc mirror invariant, the featured-first grouping, no input mutation, default sort argument), hidden-property exclusion, `getFeatured` (no limit, capped, limit 0, oversized limit, hidden exclusion), `getRelated` (current-property exclusion, sold / rented / hidden status exclusion, default cap, custom cap, deterministic order, no self-inclusion), and the combined filter + sort + pagination contract), and `app/config/agencies/agency.schema.test.ts` (73 cases — `measurementUnitSchema` (accepts both enum values, rejects unknown / empty / non-string), `agencyStructuredAddressSchema` (empty object, all fields, all optional, empty-string rejection), `agencyContactConfigSchema` (minimal / every required field rejection / optional structuredAddress / optional businessHours), `agencySocialConfigSchema` (empty object, full set, empty-string acceptance — documents the no-`min(1)` contract), `agencyModulesConfigSchema` (full set, missing field rejection, non-boolean rejection), `agencyLeadsConfigSchema` (true, false, missing, non-boolean), `agencyConfigSchema` (id / name / logo / favicon / theme / locales / currency / measurement-unit rules), `validateAgencyConfig` (happy path, defaultLocale not in availableLocales, defaultLocale not in i18nLocales, availableLocales entry not in i18nLocales, theme not in registry, structural failure on empty name, structural failure on missing contact, format warnings on email / currency / social URLs including the documented "default agency" case), and `safeParseAgencyConfig` (happy path, structural failure, cross-config failure × 3 branches, non-object input × 5 types: string, null, array, undefined, number), and a `console.warn` isolation test confirming format warnings are returned, never logged). Modified file: `vitest.config.ts` (added three new test patterns: `app/core/utils/**/*.test.ts`, `app/features/properties/services/**/*.test.ts`, and `app/config/agencies/**/*.test.ts`). No new dependencies. No runtime code changes. Total: 389 tests across 13 test files; 227 new tests added in M6 (43 paginate + 21 WhatsApp + 18 postal-address + 72 property-service + 73 agency-schema = 227 it() cases). Three consecutive `pnpm test` runs pass consistently; `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully. No source code in the test scope was modified — every test was written against the existing public contract and the existing private contract was not changed. No fixtures, no mocks, no browser environment, no coverage thresholds, no Playwright. Two concrete test failures were observed and fixed during the build (3 separate `it()` cases where my expected values were wrong: a digit miscount in two WhatsApp numbers and an incorrect assumption about the `safeParseAgencyConfig` return value being reference-equal to the input, plus one unused import). The fixes changed only the test code; no source file was modified.

35. **v1.1.0 M7 — Release preparation (Task 089).** A documentation-and-verification pass that prepares v1.1.0 for release. No new features, no new adapters, no new dependencies. New file: `docs/RELEASE_NOTES_v1.1.md` (~600 lines — the canonical v1.1.0 release notes: the headline feature (`POST /api/contact`), the four pluggable delivery adapters (`disabled` / `log` / `webhook` / `email`) with their configuration, validation, error mapping, and privacy notes; the configurable lead form (driven by `agency.leads.enabled`, defaults to `false`); the validation, honeypot, rate-limiting, and privacy behavior; the 389-test Vitest suite (per-file breakdown); static-hosting and Nitro-server deployment differences (with the verified `NUXT_PUBLIC_SITE_URL=https://example.test pnpm generate` 171-route output); known limitations; the v1.0.0 → v1.1.0 change summary; the migration path from v1.0.0). Modified files: `docs/REBRANDING.md` §12.10 (corrected the test count claim from "162 tests" to "389 tests across 13 files" and expanded the per-file table to include the M6 files and the endpoint test file; corrected the `lead.service.test.ts` count from 28 to 33 (the M4 rate-limit window-expiry cases); corrected the "what the Vitest suite does not cover" list — the endpoint transport guards ARE covered by 16 cases in `server/api/contact.post.test.ts` and the rate-limit window expiry IS covered by 5 cases in `lead.service.test.ts`; the only remaining "not covered" items are the live end-to-end tests against a real upstream and a real SMTP server, which require a live network; the live-network boundary is now documented in the release notes too), `AGENTS.md` "Current gaps — Real lead capture" (corrected the description to include the `email` adapter, the M6 broader-repo test surface, the 389-test total, and the M4 endpoint + rate-limit coverage; corrected the description to say "v1.1.0 release preparation" instead of "v1.1.0 development"), `README.md` "Deployment modes" (added `email` to the four-adapter list and changed "in development" to "ships" for v1.1.0), and `docs/ROADMAP.md` (this entry, Section 4 active task description, Section 5 active task description). The four documentation fixes were the only defects found during the review; no source code was changed. Validation: `pnpm test` passes 389/389 on three consecutive runs; `pnpm lint` reports 0 errors and 0 warnings; `pnpm build` completes successfully; `NUXT_PUBLIC_SITE_URL=https://example.test pnpm generate` prerenders 171 routes; a recursive scan of `.output/public/` for `NUXT_LEADS_*` env-var names, `leadsSmtp*` / `leadsWebhook*` / `leadsEmail*` runtime config keys, and `leadsAdapter` returns zero matches; `runtimeConfig.public` contains exactly one field (`siteUrl`); the static `/contact` page contains the form with `disabled` submit, the honeypot field, and the visible `contact.form.placeholderNotice`; `git diff --check` is clean. Total: 0 source code files changed, 4 documentation files changed, 1 new release-notes file created.

Task 083 (v1.1.0 M1) and Task 084 (v1.1.0 M2, Vitest Foundation) are complete and verified. Tasks 085 (v1.1.0 M3, documentation parity), 086 (v1.1.0 M4, endpoint + rate-limit window-expiry tests), 087 (v1.1.0 M5, SMTP email delivery adapter), 088 (v1.1.0 M6, test coverage expansion), and 089 (v1.1.0 M7, release preparation) are complete. The active task is now **Task 090 — see Section 5.** Owner: this OpenCode session.

v1.0.0 is released (tag `v1.0.0` on `release/v1.0.0` at `456284c`). v1.1.0 is in development on the `feature/lead-capture-v1.1` branch. The first seven v1.1.0 milestones (M0 — lead-capture audit, M1 — lead-capture build + post-merge verification, M2 — Vitest foundation, M3 — documentation parity, M4 — endpoint + rate-limit window-expiry tests, M5 — SMTP email delivery adapter, M6 — test coverage expansion) are complete. M6 adds 227 new tests across 5 new test files (`app/core/utils/paginate.test.ts`, `app/core/utils/whatsapp-link.test.ts`, `app/core/utils/postal-address.test.ts`, `app/features/properties/services/properties.service.test.ts`, `app/config/agencies/agency.schema.test.ts`), bringing the total to 389 tests across 13 test files. The broader-repo pure-function / branch-rich surface is now covered: pagination edge cases, WhatsApp link normalization, postal-address JSON-LD builder, property service (filter / sort / getAll / getBySlug / getFeatured / getRelated / isPropertySort), and the agency configuration schema (structural + cross-config + format warnings). The next v1.1.0 task is a future coverage or feature work — see Section 5.

v1.0.0 is released (tag `v1.0.0` on `release/v1.0.0` at `456284c`). v1.1.0 is in development on the `feature/lead-capture-v1.1` branch. The first six v1.1.0 milestones (M0 — lead-capture audit, M1 — lead-capture build + post-merge verification, M2 — Vitest foundation, M3 — documentation parity, M4 — endpoint + rate-limit window-expiry tests, M5 — SMTP email delivery adapter) are complete. M5 adds the `email` adapter using Nodemailer, with 7 private runtime config keys (`NUXT_LEADS_SMTP_*` / `NUXT_LEADS_EMAIL_*`), 35 new tests, and full HTML escaping. The total is now 162 tests across 8 test files. The next v1.1.0 task is expanding coverage to the remaining pure-function / branch-rich surface — see Section 5.


### Working tree

`git status --short` shows the in-progress v1.1.0 M7 changes (Task 089) on the `feature/lead-capture-v1.1` branch at HEAD `4e1e42d` ("Expand core utility test coverage"). The working tree adds `docs/RELEASE_NOTES_v1.1.md`, updates `docs/REBRANDING.md` §12.10 (test count + per-file table + "what's not covered" list), `AGENTS.md` "Current gaps — Real lead capture", `README.md` "Deployment modes", and this file (Sections 3, 4, 5, 8); no source code is modified. The next session does not need to commit anything before opening the final release-candidate verification.

## 5. Upcoming Tasks

v1.0.0 is released. v1.1.0 is in development. The first eight v1.1.0 milestones (M0 — lead-capture audit, M1 — lead-capture build + post-merge verification, M2 — Vitest foundation, M3 — documentation parity, M4 — endpoint + rate-limit window-expiry tests, M5 — SMTP email delivery adapter, M6 — test coverage expansion, M7 — release preparation) are complete. The next task is the final release-candidate verification, then the v1.1.0 release branch is cut from the current `feature/lead-capture-v1.1` HEAD and tagged `v1.1.0`. The remaining important untested utilities are documented in Section 6 ("Remaining important untested utilities") so they are tracked but not blocking the release.

* **Next active task: Task 090 — v1.1.0 final release-candidate verification.** A small final pass that re-runs the full v1.1.0 verification pipeline against the current `feature/lead-capture-v1.1` HEAD: `pnpm test` x3, `pnpm lint`, `pnpm build`, `NUXT_PUBLIC_SITE_URL=https://example.test pnpm generate` (the 171-route static-export check), the recursive scan of `.output/public/` for `NUXT_LEADS_*` env-var names and `leadsSmtp*` / `leadsWebhook*` / `leadsEmail*` runtime config keys, the `runtimeConfig.public` one-field invariant, the static `/contact` page placeholder-notice presence, the rebrand verification with a temporary second-agency configuration (the same pattern as the v1.0.0 final-correction commit, Task 082 / M27), and the `git diff --check` whitespace check. The `release/v1.1.0` branch is then cut from the current `feature/lead-capture-v1.1` HEAD and tagged `v1.1.0`. The `v1.0.0` tag and the `release/v1.0.0` branch are not force-pushed or re-tagged.

Future follow-up work lives in Section 6 — Deferred Work.

## 6. Deferred Work

Tracked separately so they are not lost but they are explicitly not part of the next ten tasks.

* **Fullscreen property-gallery lightbox (from M20 / M21).** The fullscreen lightbox with focus trap, body-scroll lock, Escape handler, and backdrop click remains intentionally deferred. A real-estate user wanting a larger view can use the browser's built-in image controls on the current main image. A future audit can decide whether a real-estate agency's typical photo set warrants a fullscreen lightbox and whether a generic `BaseLightbox` shared component is justified.
* **Real external data integration (from AGENTS.md).** The MVP reads from static data under `app/features/*/data/*.ts`. A CMS, API, or external image source is not wired in; the schemas are the runtime boundary that will validate the future source.
* **Per-development detail page** (`/developments/[slug]`) — `Development.slug` is reserved; the route, the development `DevelopmentCard` deep link, and the sitemap entry are all waiting on the build.
* **Per-agent detail page** (`/agents/[slug]`) — `Agent.id` is reserved; the route, the agent detail block, and the JSON-LD are waiting on the build.
* **Real lead capture** — the lead-capture pipeline is shipped in v1.1.0 (M0 / M1, M5, M7): `POST /api/contact` with four pluggable server-only delivery adapters (`disabled`, `log`, `webhook`, `email`), a shared Zod schema, a 16 KB body limit, a 5-per-10-minute per-process rate limit, a honeypot, and full PII-redacted logging. The release notes are in `docs/RELEASE_NOTES_v1.1.md`. One natural follow-up remains intentionally deferred: a **distributed rate limiter** that survives multi-process deployments (PM2 cluster, Cloudflare Workers isolates). The current `Map`-based limiter is per-process and is documented as such in `docs/REBRANDING.md` §12.8.
* **Vitest unit-test foundation** — the lead-capture surface (Zod schema, four delivery adapters, adapter selector, lead service, endpoint transport, rate-limit window expiry) is covered by 162 tests across 8 files (M2 / M3 / M4 / M5). The broader-repo pure-function / branch-rich surface is covered by 227 additional tests across 5 files in M6: `paginate` / `parsePageParam` (43 cases), `buildWhatsAppLink` (21), `agencyPostalAddress` (18), `propertiesService` filter / sort / getAll / getBySlug / getFeatured / getRelated / isPropertySort (72), and the `agencyConfigSchema` structural / cross-config / format-warnings surface (73). Total: 389 tests across 13 files. The next coverage-expansion pass (v1.1.0 M8+, Task 090+) targets the remaining important untested utilities documented in the "Remaining important untested utilities" subsection below.
* **Playwright smoke tests + CI** — smoke tests for the 7 routes, the filter form, the pagination, the gallery, the breadcrumb, and the contact form (placeholder and enabled). No runner, no GitHub Actions workflow, no `.gitlab-ci.yml` are present.
* **Blog / content module** — `agency.modules.blog` ships `false` by default; the route and the home section are not implemented.
* **Testimonials module** — `agency.modules.testimonials` ships `true`; the section is implemented (`HomeTestimonials`) but has no dedicated route or admin.
* **Property comparison, saved properties, accounts, scheduling, valuation, PDF brochures** — all future-phase features from `docs/SPEC.md` Section 7 / `docs/ROADMAP.md` (legacy) Section 5.
* **Admin / dashboard / auth / CMS / CRM / maps / external image storage / payments** — explicitly out of MVP scope per `docs/SPEC.md` Section 7.
* **Dark mode** — `docs/DESIGN.md` calls dark mode optional for the MVP and the token system already supports it via a `[data-theme='dark']` selector, but no implementation exists.
* **Multi-agency / multi-tenant deployment** — `docs/REBRANDING.md` describes the path, but the runtime still resolves a single `activeAgency` at boot.

### Remaining important untested utilities

These are pure-function or branch-rich utilities that the v1.1.0 M6 coverage pass (Task 088) did not reach. They are listed here so a future coverage pass (Task 089 or later) has a concrete scope. Each entry records the file path, the surface the future tests should cover, and a one-line reason it was deferred from M6.

* **`app/core/utils/currency-format.ts` — `formatCurrency`.** The active currency formatter. The M6 pass deliberately deferred it because it delegates to `Intl.NumberFormat`, which makes a true assertion-only test (without locale snapshots) noisy and brittle. A future pass can cover: the happy path (`USD` / `en-US`, `MXN` / `es-MX`), the maximum-fraction-digits clamp, the `try`/`catch` fallback when the currency code is unknown (returns `${amount} ${currency}`), and the default-locale fallback. A reasonable minimum: 6–8 cases. The `Intl.NumberFormat` call itself is out of scope (browser / runtime contract, not the template's code).
* **`app/core/utils/theme-to-css-vars.ts` — `themeToCssVars`.** The theme token → CSS variable serializer used during SSR. A future pass can cover: the `:root{...}` wrapper, every documented CSS variable (color / radius / shadow / font / layout), the key/value separator, and the empty-token-set edge case. A reasonable minimum: 4–6 cases.
* **`app/core/composables/useJsonLd.ts` — `useJsonLd`.** The JSON-LD `<script type="application/ld+json">` emitter. A future pass would need to mock `useHead` from `#imports` (the existing `tests/stubs/imports.ts` provides the stub; `vi.mock('#imports', ...)` can extend it). The surface is small (one `useHead` call with a single script entry) but the test would be the first one in the repo to mock `useHead`, so the infrastructure is the real scope. A reasonable minimum: 3–4 cases (plain object, ref, getter, JSON serialization).
* **`app/core/composables/usePageSeo.ts` — `usePageSeo`.** The page-level SEO composable. The deferred reason is the same as `useJsonLd`: it depends on `useI18n`, `useRoute`, `useRuntimeConfig`, and `useSiteConfig` from `#imports` plus `~/composables/useSiteConfig` and `~/config/seo`, none of which are mocked in any existing test. A future pass would need either (a) a deeper `#imports` mock set or (b) a Pinia test harness, both of which are a real piece of infrastructure. The composable has a rich pure surface (`toAbsoluteUrl` already has its own documented branches: empty path, absolute URL, missing `siteUrl`, leading-slash normalization, trailing-slash on `siteUrl`); the test scope is small but the setup is non-trivial. A reasonable minimum: 8–10 cases focused on `toAbsoluteUrl` (the only pure function) and a smaller integration test of the `computed` properties.
* **`app/features/*/schemas/*.schema.ts` (other than `lead.schema.ts` and `property.schema.ts`).** The development / agent / contact / home schemas. These are small, stable Zod shapes, but the M6 pass kept the scope to the existing lead + property schemas plus the agency schema. A future pass can cover the remaining schemas with the same pattern used for `property.schema.ts`. A reasonable minimum: 10–20 cases total.
* **`server/routes/sitemap.xml.ts` and `server/routes/robots.txt.ts`.** The two dynamic Nitro routes. The deferred reason is the same as `useJsonLd` / `usePageSeo`: testing Nitro routes requires either a full Nitro runtime or a deeper `h3` mock set than `tests/helpers/h3-event.ts` provides. A future pass can extend the helper to support the `defineEventHandler` shape and add a small set of cases per route. A reasonable minimum: 4–6 cases per route (gated by `agency.modules.*`, empty sitemap, the sitemap entry shape, the robots-txt gating by `NUXT_PUBLIC_SITE_URL`).
* **`server/services/leads/lead.service.ts` — `getRequestKey` and the internal helpers.** The lead service's public surface is covered (rate limit, stamping, delivery mapping), but the small private helpers (`getRequestKey`, the rate-limit cleanup, the `getForwardedBody` chain) are tested only through the public API. A future pass can lift the helpers to a small `server/services/leads/utils/` module and test them directly. A reasonable minimum: 4–6 cases.

Each of these is a discrete, well-bounded future task. The list is intentionally ordered by "smallest infrastructure cost first" so a future pass can pick the cheapest one to make incremental progress.

## 7. Important Architectural Decisions

These are the decisions a new session needs to know to avoid re-litigating them. Each is enforced by the codebase; do not undo them without updating the relevant `docs/` file.

* **Nuxt 4 feature-first architecture** under `app/features/<domain>/{api,components,composables,constants,data,schemas,services,stores,types,utils}/`. Shared infrastructure lives under `app/components/{ui,layout,shared}/`, `app/core/`, `app/config/`, `app/themes/`, and `app/composables/`. See `docs/ARCHITECTURE.md` and `AGENTS.md` "Architecture".
* **Agency branding is configuration-driven.** All agency identity, contact details, social links, currency, measurement unit, and enabled modules live in `app/config/agencies/*.agency.ts` and are read through `useSiteConfig()`. Components never hardcode agency content. See `docs/REBRANDING.md`.
* **Theme tokens are CSS variables, not Tailwind palette classes.** The active theme is resolved by `app/themes/index.ts` and serialized into `var(--color-*)`, `var(--radius-*)`, `var(--shadow-*)`, `var(--font-*)` on `<html data-theme="…">`. Components use the variables; Tailwind color utilities are not used for brand colors.
* **i18n keys everywhere.** No hardcoded visible UI text. Every `$t('key')` is defined in both `i18n/locales/en.json` and `i18n/locales/es.json`.
* **Listing filters use URL query parameters.** `/properties?operation=&type=&location=&sort=`. The `applyFilters` handler in the page only navigates with non-empty / non-default values, and the canonical URL strips the query string for SEO. Documented in `docs/DATA_MODELS.md` Section 9 and `docs/REBRANDING.md` Section 11.
* **Location matching is case-insensitive and accent-insensitive** (Unicode NFD + combining-mark strip, no external dependency). Implemented in `propertiesService.filter`. Documented in `docs/DATA_MODELS.md` Section 9.
* **Default sorting is stable across SSR and CSR.** Every sort branch uses `id.localeCompare(otherId)` as a tiebreaker so equal-scoring or equal-priced properties render in the same order in both passes. The `ItemList` JSON-LD mirrors the visible order via `position: index + 1`.
* **Mobile filters use an inline collapse, not a modal sheet.** A `< sm` viewport collapses the filter form behind a "Filters" toggle button that reflects the active filter count. The form keeps `method="get" action="/properties"` so it still submits natively without JavaScript.
* **LCP images use `loading="eager"` + `fetchpriority="high"`** (the home hero in `HomeHero.vue` and the property detail cover inside `PropertyGallery.vue`). All other images rely on `<ResponsiveImage>`'s `lazy` / `auto` defaults. The wrapper forwards both attributes to `<NuxtImg>`. Documented in `docs/REBRANDING.md` Section 12.
* **JSON-LD uses stable canonical entity identifiers.** The home page's `RealEstateAgent.@id` is the absolute home URL; the contact page's `ContactPage.mainEntity` and the about page's `AboutPage.mainEntity` reuse the same `@id` so search engines treat the agency as a single knowledge-graph node. Per-listing `RealEstateListing` uses `canonicalUrl` as `@id` when `NUXT_PUBLIC_SITE_URL` is set.
* **Property sample data is validated through Zod** at module load via `propertyListSchema` in `app/features/properties/schemas/property.schema.ts`. A bad record prevents the app from booting — this is intentional.
* **Empty required property strings are rejected by the schema.** `coverImage`, `title`, `slug`, `description`, `location`, `city`, `state`, `country`, `currency`, and every `images[i]` / `amenities[i]` entry are `.min(1)`. The agency config has its own validation in `app/config/agencies/default.agency.ts`.
* **Global stores are reserved for app-wide state only.** `app/stores/` is a `.gitkeep`. The current MVP has no global Pinia store; per-feature stores would live under their feature folder. See `docs/ARCHITECTURE.md` Section 7.
* **`server/routes/` is the right home for Nitro endpoints** (current files: `sitemap.xml.ts`, `robots.txt.ts`). The `app/server/` path does not exist; do not create it.
* **ESLint is auto-resolved by `@nuxt/eslint`.** Do not add a manual ESLint config.
* **Pagination is in v1.0 scope regardless of the placeholder catalog size.** The shipped `app/features/properties/data/properties.ts` is placeholder content for a single template demonstration, not a production agency. The template must be production-ready for any agency on day one. `BasePagination` and the `paginate` utility shipped in M17 are not gated on the placeholder catalog reaching a threshold — the control renders `null` when `totalPages <= 1`, so the current 6-record catalog shows no pagination UI at all. Audit decision recorded in M14.
* **Breadcrumbs are in v1.0 scope for the property detail page only.** Listing pages and top-level pages do not get visible breadcrumbs in v1.0 because they are themselves top-level destinations. The `SeoBreadcrumbs` shared component + `BreadcrumbList` JSON-LD payload shipped in M16 will be reused unchanged by the future `/agents/[id]` and `/developments/[slug]` detail pages — each one only needs a different three-item `items` array. Audit decision recorded in M15.
* **Agency addresses use an optional `AgencyStructuredAddress` companion that drives `PostalAddress` JSON-LD.** The free-text `AgencyContactConfig.address` stays the visible display value (footer + contact card) and is unchanged and required; the optional `structuredAddress` is the schema.org `PostalAddress` payload consumed by the JSON-LD builder on the home, contact and about pages. The three agency `RealEstateAgent` nodes still share the same `toAbsoluteUrl('/')` `@id` so the knowledge-graph node identity is preserved. Property `PostalAddress` is intentionally deferred — the placeholder property catalog has no street addresses or postal codes, and the existing `RealEstateListing` payload documents this as a deliberate omission. Audit and build decisions recorded in M18 and M19.
* **The property gallery uses Swiper v12 (existing dependency) for the main carousel and the preserved custom thumbnail strip.** The first image is the property detail page LCP candidate; the SSR HTML contains it via a `<ClientOnly>` `#fallback` slot as a plain `ResponsiveImage` with `loading="eager"` and `fetchpriority="high"`, and the Swiper replaces it after hydration using the same `src` (browser cache prevents a duplicate download). The first slide inside the Swiper also carries `loading="eager"` and `fetchpriority="high"`; later main images and all thumbnails are `loading="lazy"`. The custom real `<button type="button">` previous and next controls are wired to the Swiper `Navigation` module's `prevEl`/`nextEl` and carry the native `disabled` attribute at the boundaries (no loop, no rewind). Keyboard arrow navigation is enabled only when the carousel is in the viewport. `prefers-reduced-motion: reduce` sets Swiper's transition `speed` to 0 via VueUse's `usePreferredReducedMotion` composable. The fullscreen lightbox is **intentionally deferred** to a future audit: focus trap, body-scroll lock, Escape handler, and backdrop click are not justified by the current placeholder data, and introducing a generic modal system solely for the gallery is out of scope. Audit and build decisions recorded in M20 and M21.

## 8. How to Resume Work in a New Session

1. **Read `AGENTS.md`** for the project-wide rules (stack, conventions, naming, validation).
2. **Read `docs/OPENCODE.md`** (now includes the "Roadmap-first rule" in Section 0.1) and **`docs/ROADMAP.md` (this file)**. The roadmap is the canonical entry point for what is done, what is active, and what is next.
3. **Read the task-specific doc** in `docs/` for the task you are about to start. For the active task (Task 090 — v1.1.0 final release-candidate verification, v1.1.0 M8+), the relevant docs are `docs/RELEASE_NOTES_v1.1.md` (the canonical v1.1.0 release notes), `docs/REBRANDING.md` §12 (the lead-capture rebrand section), `docs/DATA_MODELS.md` §5 (the lead-capture data model and endpoint response table), and the v1.0.0 final-correction commit pattern (Task 082 / M27 — re-verification with a temporary second-agency configuration, the static-export verification, and the rebrand verification with `NUXT_PUBLIC_SITE_URL=https://example.test pnpm generate`). The lead-capture surface is covered by 162 tests (M2 / M3 / M4 / M5) and the broader-repo pure-function / branch-rich surface is covered by 227 additional tests (M6); the M7 release-preparation pass added `docs/RELEASE_NOTES_v1.1.md` and corrected 4 documentation defects; no source code was changed in M7. The final release-candidate verification runs the v1.0.0 / M27 verification pipeline against the current `feature/lead-capture-v1.1` HEAD, the `release/v1.1.0` branch is then cut from this commit, and the `v1.1.0` tag is added. The `v1.0.0` tag and the `release/v1.0.0` branch are not force-pushed or re-tagged.
4. **Check `git status` and `git log --oneline -10`** to confirm the working tree state and the latest commits.
5. **Reuse existing primitives** — `usePageSeo`, `useJsonLd`, `useSiteConfig`, `propertiesService`, `BaseButton`, `BaseIcon`, `BaseSection`, `SectionHeader`, `CtaBlock`, `ResponsiveImage`, etc. — instead of building parallel helpers.
6. **Update i18n in both locales** when adding or changing visible strings.
7. **Run `pnpm lint` and `pnpm build`** before declaring the task done.
8. **Update this file** (see Section 9) so the next session starts from an accurate state.

## 9. Roadmap Update Policy

The roadmap is meant to be easy to keep current. After each completed task:

* Move the task from "Current work" to the matching milestone in Section 3, or add a new milestone if the work is large enough to deserve one. Do not list every historical commit — summarise by milestone.
* If a task is deferred, move it from "Upcoming tasks" to "Deferred work" with a one-line reason.
* If a new task is identified, append it to "Upcoming tasks" (preserving the order; new tasks go to the end of the queue, not the front).
* If an architectural decision changes, update Section 7 and the matching `docs/` file in the same change.
* Do not duplicate implementation details here. Implementation details belong in the JSDoc of the affected source file or in the relevant `docs/` file. This file is the index, not the documentation.
* Do not paste full task outputs, full diffs, or full file contents into this file. A short status line per milestone is enough.
